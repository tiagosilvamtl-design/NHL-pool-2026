================================================================================
 NHL PLAYOFF POOL — PROJECT STATUS
 Last updated: 2026-05-01
================================================================================

OVERVIEW
--------
A free, automated NHL playoffs predictions pool hosted on GitHub Pages.
Friends submit picks via a web form (no login required). Picks lock
automatically when each series starts. Scores update hourly via the NHL
public API. No ongoing cost — everything runs on free tiers.

Live URL: https://tiagosilvamtl-design.github.io/NHL-pool-2026/


--------------------------------------------------------------------------------
SCORING RULES
--------------------------------------------------------------------------------
  +2 pts  Correct series winner
  +1 pt   Correct number of games (4/5/6/7)
          — only awarded if the winner pick is also correct


--------------------------------------------------------------------------------
ARCHITECTURE
--------------------------------------------------------------------------------

  [GitHub Pages]          Hosts the static frontend (HTML/CSS/JS)
       ↓ GET requests
  [Google Apps Script]    Backend API — reads/writes the Google Sheet
       ↓ read/write
  [Google Sheets]         Database — stores picks and series results
       ↑ POST (hourly)
  [GitHub Actions]        Fetches NHL API, pushes results to Apps Script

  NHL API (free, no auth):
    - Bracket:  https://api-web.nhle.com/v1/playoff-series/carousel/20252026/
    - Schedule: https://api-web.nhle.com/v1/schedule/YYYY-MM-DD


--------------------------------------------------------------------------------
INFRASTRUCTURE SETUP (completed)
--------------------------------------------------------------------------------

  Google Sheet ID:    1KBhVCvtmN2LQAeTcbQhqCE5x1uZDIExgqeeAJIlq5ZA
  Apps Script URL:    https://script.google.com/macros/s/AKfycbwsS25ehzWAZ5J5oWjNprU1atsy50FNZIwSjSLZTfPcPfWFFfyIGScgqOk53Mwa4G6q6w/exec
  GitHub Repo:        https://github.com/tiagosilvamtl-design/NHL-pool-2026
  GitHub Pages:       https://tiagosilvamtl-design.github.io/NHL-pool-2026/

  GitHub Secrets configured:
    - APPS_SCRIPT_SECRET   (shared secret between Actions and Apps Script; value: Alakazam)

  GitHub Variables configured:
    - APPS_SCRIPT_URL      (Apps Script /exec URL)

  Google Sheets tabs:
    - submissions   timestamp | name | email | series_id | pick_team | pick_games
    - series        series_id | round | team1_abbr | team2_abbr | team1_name |
                    team2_name | winner_abbr | actual_games | first_game_utc |
                    locked | status | team1_logo | team2_logo | conference |
                    team1_wins | team2_wins
    - pins          email | pin_hash | created_at


--------------------------------------------------------------------------------
FILE STRUCTURE
--------------------------------------------------------------------------------

  index.html                      Leaderboard page
  overview.html                   Picks overview — participant × series matrix
  picks.html                      Pick submission form
  bracket.html                    Playoff bracket view
  css/style.css                   All styling (includes mobile responsive)
  js/config.js                    Apps Script URL constant
  js/api.js                       Fetch wrappers for backend + NHL API
  js/leaderboard.js               Leaderboard rendering + auto-refresh + modal
  js/overview.js                  Overview matrix rendering
  js/picks.js                     Picks form logic, lock enforcement, pre-fill
  js/shame.js                     Easter egg animations (shame + quips)
  js/bracket.js                   Bracket visualization with live series scores
  gas/Code.gs                     Google Apps Script backend (copy into editor)
  gas/Setup.gs                    One-time sheet setup utility
  .github/scripts/update-scores.js   Node script — fetches NHL API
  .github/workflows/update-scores.yml  Hourly GitHub Actions workflow
  README.md                       Setup instructions
  project_status.txt              This file


--------------------------------------------------------------------------------
KEY BEHAVIOURS
--------------------------------------------------------------------------------

  Submission
    - Anyone with the link can submit picks — no login required
    - Identified by email address (unique per person)
    - First submission sets a 4–6 digit PIN (stored as SHA-256 hash)
    - Returning users must enter their email + correct PIN to load/update picks
    - Name auto-fills from prior submission on PIN verification
    - Picks can be updated until the series locks
    - Each series locks independently when its first game starts
    - On re-submission with same email + correct PIN, existing picks are overwritten

  Locking
    - Lock time is determined from the NHL schedule API (first game UTC time)
    - Lock status is computed by GitHub Actions hourly and stored in the Sheet
    - Locked series show a "LOCKED" badge; inputs are disabled on the form

  Scoring (computed live by Apps Script on every leaderboard load)
    - Scans all completed series against all submissions
    - +2 for correct winner, +1 bonus for correct games (winner must be correct)
    - Leaderboard sorted by total, ties share rank

  Automation
    - GitHub Actions runs every hour
    - Fetches NHL carousel API for series results and team logos
    - Fetches NHL schedule API to determine first game times
    - POSTs results to Apps Script with secret token authentication
    - Apps Script upserts the series sheet

  Easter eggs
    - Picking against the Montreal Canadiens triggers a full-screen "SHAME"
      animation (falling text, bell ringer) after submission
    - Clicking Colorado fires: "Caufield is the ethical Rocket" 🚀
    - Clicking Boston Bruins fires: "Hit him with the stick!" 🏒
    - COL/BOS quips trigger on team card click, auto-dismiss after 3 seconds


--------------------------------------------------------------------------------
REDEPLOYMENT NOTES (for future changes to Code.gs)
--------------------------------------------------------------------------------

  Every time gas/Code.gs is modified:
    1. Open Google Sheet → Extensions → Apps Script
    2. Replace Code.gs content with the updated version from the repo
    3. Save (Cmd+S)
    4. Deploy → Manage deployments → pencil icon → New version → Deploy
    The /exec URL stays the same — no need to update config.js or GitHub vars.


--------------------------------------------------------------------------------
CHANGE LOG
--------------------------------------------------------------------------------

  2026-04-15  Initial build
              - Full project scaffolded and deployed
              - GitHub Pages live, Apps Script deployed, GitHub Actions running
              - Google Sheet populated with 8 first-round series

  2026-04-16  PIN protection + East/West bracket redesign
              - PIN field added to picks form (set on first submit, required to update)
              - PINs stored as SHA-256 hashes in new `pins` sheet (never plaintext)
              - check_email GAS action returns whether email already has a PIN
              - conference column added to series sheet (position 14)
              - update-scores.js extracts conference from NHL API (letter fallback)
              - bracket.html redesigned: East left, West right, SCF in center
              - Fallback flat layout used until conference data is populated
              - GAS Setup.gs updated with addConferenceHeader() and addPinsSheet()

  2026-04-16  Visual redesign + logo bug fix
              - Added team1_logo / team2_logo columns to series sheet
              - Redesigned picks form: large logo-centric team selection cards
              - New deep navy color system with smooth transitions
              - Leaderboard: gold/silver/bronze medal accents for top 3
              - Bracket: divider lines, start time display, winner highlight
              - Skeleton shimmer loading states

  2026-04-17  NHL API field name fix
              - Fixed critical bug: update-scores.js used s.topSeedTeam /
                s.bottomSeedTeam but NHL API returns s.topSeed / s.bottomSeed
              - Added commonName object guard (API may return localized object)
              - Added one-shot debug logging of API field names after first run
              - Team data (abbrev, name, logo) now populates correctly in sheet

  2026-04-17  Bracket improvements
              - All 7 columns always render regardless of data availability
              - Future rounds show dashed TBD placeholder cards
              - Vertical alignment via space-around: R2 cards auto-center
                between their R1 feeders, CF centers between R2 pairs

  2026-04-17  Picks security + UX improvements
              - Picks no longer pre-fill on email blur (security fix)
              - Picks now load only after correct PIN is entered (PIN blur)
              - Server-side PIN validation added to get_my_picks GAS action
              - Name auto-fills from prior submission on PIN verification
              - Confirmation banner scrolls into view after submit
              - Instruction text updated to explain how to edit picks

  2026-04-17  Easter eggs
              - COL pick: "Caufield is the ethical Rocket" 🚀 (on click)
              - BOS pick: "Hit him with the stick!" 🏒 (on click)
              - Lightweight centered quip overlay, auto-dismisses in 3s

  2026-04-17  Mobile responsive improvements
              - Bracket stacks vertically on ≤900px screens: rounds pile
                top-to-bottom (R1→R2→CF) for each conference, West reversed
                so R1 also reads at top; SCF gets horizontal gold borders
              - General (≤640px): tighter padding, leaderboard table scrolls
                horizontally, picks grid goes single-column, logos shrink
              - Very small phones (≤400px): site title hidden for nav room

  2026-04-18  Leaderboard: click name to view picks modal
              - Clicking any participant name on the leaderboard opens a modal
                showing their picks for every locked/completed series
              - Picks only visible once locked (no spoiling open picks)
              - Each pick row shows: matchup, team chosen, games predicted,
                ✓/✗ result + point count for completed series, "In progress"
                badge for locked-but-running series
              - Modal dismisses on outside click or Escape key
              - getLeaderboard() in Code.gs extended:
                  · Returns series array with team logos, names, status
                  · Adds locked-active picks to picksDetail (correct_winner: null)
              - Requires Code.gs redeploy

  2026-04-18  Overview page (participant × series matrix)
              - New overview.html page added to nav
              - Renders a scrollable table: participants (rows) × locked series
                (columns), grouped by round
              - Cell colours: green = correct winner, red = wrong, blue tint =
                in progress, blank = no pick
              - Series headers show both team logos with winner highlighted
              - Sticky first column keeps participant names visible on scroll
              - getLeaderboard() response reused (no new backend endpoint)

  2026-04-19  Overview desktop alignment fixes
              - Removed negative margin on .ov-table-wrap (sticky column offset)
              - Added vertical-align: middle to all .ov-table th/td
              - Fixed .ov-series-header vertical-align (bottom → middle)
              - Reduced name column padding to push "Participant" label left

  2026-04-19  Live series scores in bracket
              - update-scores.js now stores team1_wins / team2_wins per series
                (from topSeed.wins / bottomSeed.wins in NHL carousel API)
              - Code.gs: SER_COLS extended with team1_wins (col 14) and
                team2_wins (col 15); rowToSeries and updateResults updated
              - bracket.js: win count shown next to each team in card;
                status line shows "MIN leads 1-0" / "Tied 1-1" for
                in-progress series instead of generic "In progress"
              - Requires Code.gs redeploy + one Actions run to populate cols


--------------------------------------------------------------------------------
CURRENT STATE (as of 2026-05-01)
--------------------------------------------------------------------------------

  GitHub Actions:  running every hour, all green
  Series tracked:  11 (8 × Round 1 complete, 3 × Round 2 detected)
  Round 2 start:   R2J first game 2026-05-03T00:00Z; R2K/R2L TBD in schedule
  Data pipeline:   NHL API → update-scores.js → Apps Script → Google Sheet ✓


--------------------------------------------------------------------------------
KNOWN ISSUES / PENDING
--------------------------------------------------------------------------------

  - Node.js 20 deprecation warning in GitHub Actions. Update workflow to
    node-version: '24' before June 2, 2026 when GitHub forces Node 24.

  - Code.gs must be manually redeployed in Apps Script editor after every
    change to gas/Code.gs in the repo. See redeployment notes above.
    PENDING: last Code.gs change (team1_wins/team2_wins columns) has NOT
    been redeployed yet — bracket live scores will show "In progress" until
    the redeploy + one Actions run.

================================================================================
